/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  experimental: {
    // 🚫 Do NOT use "allowedDevOrigins" in 15.x – it’s not supported yet.
  },
};

module.exports = nextConfig;
